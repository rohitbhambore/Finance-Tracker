# User Service - Finance Tracker

## Run locally (requires PostgreSQL)

1. Build:

2. Run:

## Docker (recommended with infra/docker-compose)

- See `infra/docker-compose.yml` at project root. Ensure env variables are set there.

## Endpoints
- POST /api/auth/register
- POST /api/auth/login
